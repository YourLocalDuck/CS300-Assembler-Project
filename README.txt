This assembler was written in Java, entirely on the IU Tesla server.

When running this assembler, we recommend using Tesla.

To compile the entire project, type "make assembler"

To run, type "make run"

